version: 0.2

env:
  shell: bash

phases:
  build:
    commands:
      - set -e
      - env | grep CODEBUILD_SRC_DIR
      - input=CODEBUILD_SRC_DIR_alarms_$AWS_ACCOUNT_ID
      - cd ${!input}
      - ls -l
