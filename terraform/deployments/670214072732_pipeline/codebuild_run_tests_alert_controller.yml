version: 0.2

env:
  shell: bash

phases:
  pre_build:
    commands:
      - |
        set -c
        set -e
        cd lambda/alert_controller
        pip install -r requirements-dev.txt
  build:
    commands:
      - |
        set -c
        set -e
        make test
        echo "Linting and tests passed"
